# 2026-09-25

## 主题页脚品牌化 + 备案自定义 HTML

- 用户把页脚 "Theme by Komari Naive" 改为 **MakerStudio**，链接指向其 fork：https://github.com/hn1318/komari-theme-naive-modify （仅改 Theme by 行，Powered by Komari Monitor 保留）。
- 在 `komari-theme.json` 的「备案设置」分组新增 `customFilingHtml`（type: richtext），用于在页脚备案区域插入任意 HTML 片段（如 UptimeRobot 状态徽章）。
- `src/stores/app.ts` 新增 `customFilingHtml` computed；`src/components/Footer.vue` 中 `showFiling` 增加该字段判断，备案区域用 `v-html` 渲染（管理员可控、信任来源）。
- `pnpm build` 通过，产物 `komari-theme-naive-build-unknown.zip`（git hash 为 unknown 因仓库非 git 环境）。

## 新增全球地图视图（第三种节点视图）
- 页脚工具栏新增地球图标按钮（i-icon-park-outline-earth），节点视图模式新增 `map`（卡片/列表/地图三选一）。
- `src/components/NodeMap.vue`（新建）：基于 echarts `geo` + `effectScatter`，按地区 ISO 代码聚合节点并在世界地图标注；标记颜色按在线状态（全在线绿/全离线红/部分在线黄），tooltip 显示地区名/节点数/在线数，点击跳转该地区首个节点详情。组件经 `defineAsyncComponent` 懒加载，世界地图 GeoJSON 仅地图视图加载。
- `src/utils/regionGeo.ts`（新建）：由 world-countries 数据生成的 ISO 3166-1 alpha-2 -> [经度,纬度] 坐标表（250 条）。
- `src/assets/world.geo.json`（新建）：echarts v4 世界地图 GeoJSON（~1MB）。
- `src/utils/echarts.ts`：注册 `GeoComponent` + `EffectScatterChart`。
- `src/stores/app.ts`：`NodeViewMode` 增加 `'map'`，`defaultViewMode`/`isValidViewMode` 同步放行；`komari-theme.json` 的 `defaultViewMode` options 增加 `map`。
- `pnpm build` 通过（type-check + build）；地图 chunk 约 1MB，懒加载不影响首屏。
